/*
import 'package:camera/camera.dart';
import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

import '../Overlays/GameWithCameraOverlay.dart';

class FostiatorApp extends StatelessWidget {
  final CameraDescription camera;

  const FostiatorApp({Key? key, required this.camera}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      home: GameWithCameraOverlay(camera: camera),
    );
  }
}

 */